% ==============================================================================
%  Appendix A -- Notation and lattice conventions
%  Purpose: the single normative table of conventions, kept in sync with the
%  code (they are the same conventions as the repo's CLAUDE.md).
% ==============================================================================

\section{Notation}
\label{app:notation}

\TODO{Symbol table: $U_\mu(x)$, $P_{\mu\nu}(x)$, $\Omega(x)$, $T_{\Delta x}(x)$,
$\Obar(t)$, $C(\Delta)$, $\meff$, $A_0$, $\beta$, $\xi$, $\at$, $\as$,
$R$ (transport radius), $D$, $L$, $L_t$, $N_c$.}

\section{Lattice conventions}
\label{app:conventions}

\TODO{The normative list (mirrors the code):
plaquette $P_{\mu\nu}(x) = U_\mu(x) U_\nu(x+\hat\mu) U^{\dg}_\mu(x+\hat\nu)
U^{\dg}_\nu(x)$ with $\mu < \nu$ lexicographic ordering;
Wilson action $S = \beta \sum_p (1 - \Re\Tr P / N_c)$;
time is lattice axis 0, spatial axes $1 \dots D-1$;
periodic boundary conditions;
tensor layouts -- links $(D, L, \dots, L, n_c, n_c)$, plaquettes
$(D(D-1)/2, L, \dots, L, n_c, n_c)$; color axes always present, including
$n_c = 1$ for $\mathbb{Z}_2$.}
