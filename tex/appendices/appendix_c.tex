% ==============================================================================
%  Appendix C -- Codebase and reproducibility
%  Purpose: map thesis claims to code, and record everything needed to
%  reproduce the numbers (a committee member should be able to rerun Ch. 7).
% ==============================================================================

\section{The \texttt{gelt} package}
\label{app:code-layout}

\TODO{Module map (one paragraph each): \texttt{lattice.py} (groups,
plaquettes, action, topology, transport), \texttt{sampler.py}
(Metropolis, heat-bath + OR, anisotropy, $\tau_{\mathrm{int}}$),
\texttt{glueball.py} (APE, correlators, GEVP, cosh fits, $A_0$),
\texttt{blocks\_rope.py} / \texttt{blocks\_bias.py} (GELT),
\texttt{lcnn.py} (L-CNN baseline), \texttt{cnn\_baseline.py},
\texttt{data.py}. Entry-point scripts and the pytest suite; which test
guards which thesis claim (cross-reference Appendix B).}

\section{Production runs and hyperparameters}
\label{app:code-runs}

\TODO{One table per production result: ensemble parameters
($L = 12$, $L_t = 24$, $\beta = 2.4$, $\xi = 3.0$, $N = 2000$;
$n_{\mathrm{skip}}$ from the $\tau_{\mathrm{int}}$ pre-flight; sampler =
heat-bath + $n_{\mathrm{or}}$ OR), GELT hyperparameters for Run 5
(smear levels $(0,2,4,6)$, depth, heads, $d_{qkv}$, optimizer, epochs),
seeds and the \texttt{RUN\_TAG} artifact-naming scheme, fit window
$\Delta \in [2,7]$, test split size (400 configs). Hardware: V100 for
sampling/training, CPU for the offline fits.}

\section{Reproduction recipe}
\label{app:code-repro}

\TODO{The command sequence:
\texttt{check\_glueball\_autocorrelation.py} $\to$
\texttt{measure\_glueball.py} (classical anchor, caches the ensemble)
$\to$ \texttt{train\_glueball.py} (Rayleigh training; env overrides
\texttt{GLUEBALL\_ENSEMBLE\_SEED} / \texttt{GLUEBALL\_INIT\_SEED} /
\texttt{GLUEBALL\_EVAL\_ONLY}) $\to$
\texttt{fit\_glueball\_overlap.py} (cosh fits + $A_0$ from the test-split
$\Obar$ dump). The replication batch:
\texttt{overnight\_replication.sh}.}
