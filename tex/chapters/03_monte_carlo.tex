% ==============================================================================
%  Chapter 3 -- Monte Carlo methods for lattice fields
%  Purpose: the ensemble-generation half of the toolbox, ~10-15 pages.
%  Everything here is IMPLEMENTED and VALIDATED (gelt/sampler.py,
%  validate_sampler_{z2,su2}.py, validate_anisotropy.py,
%  check_glueball_autocorrelation.py) -- this chapter mostly reports.
%  Sources: notes/sampling.md, glueball_report sec. "Ensemble generation
%  and error analysis".
% ==============================================================================

\section{Importance sampling and Markov chains}
\label{sec:mc-markov}

\TODO{Why the path integral cannot be done by uniform sampling; importance
sampling w.r.t.\ $e^{-S[U]}$; Markov chains, detailed balance, ergodicity.
Contrast with the Haar-uniform ensemble used as a $\beta$-independent
sanity check in \cref{ch:regression}.}

\section{The Metropolis algorithm}
\label{sec:mc-metropolis}

\TODO{Single-site Metropolis with checkerboard vectorisation (why the
checkerboard: parallel updates only touch non-interacting sites through
the staple). Proposals per group: $\mathbb{Z}_2$ sign flip $U \to -U$;
$\SU{2}$ near-identity left multiplication $U \to VU$. The staple sum as
the only action difference needed.}

\subsection{Validation}
\TODO{Report the four-panel sampler validations
(figures \texttt{sampler\_validation\_z2.png},
\texttt{sampler\_validation\_su2.png}): thermalisation, 2D $\beta$-scan
against the EXACT mean plaquette ($\tanh\beta$ for $\mathbb{Z}_2$,
$I_2(\beta)/I_1(\beta)$ for $\SU{2}$), the 3D $\mathbb{Z}_2$ transition
near $\beta_c \approx 0.761$ vs.\ the $\SU{2}$ confining crossover, and
plaquette autocorrelation.}

\section{Heat-bath and overrelaxation for \texorpdfstring{$\SU{2}$}{SU(2)}}
\label{sec:mc-heatbath}

\TODO{Why Metropolis is not enough for spectroscopy (critical slowing,
tuned acceptance). $\SU{2}$ heat-bath: staple decomposition $A = kV$,
Creutz sampling of the scalar quaternion component \cite{creutz1980},
$U' = W V^{\dg}$. Overrelaxation $U' = V^{\dg} U^{\dg} V^{\dg}$:
microcanonical, action-preserving to machine precision (unit-tested),
needs re-projection onto the group. Production recipe: 1 heat-bath +
$n_{\mathrm{or}}$ OR sweeps. Note the replication-run lesson: the Creutz
rejection loop needs a generous iteration cap (acceptance
$\sim \sqrt{\pi/2a}$ collapses for the large temporal staples of an
anisotropic lattice -- cap raised $100 \to 1000$ after a production run
died on it). SU(2)-only; SU(3) needs Cabibbo--Marinari
(\cref{sec:concl-future}).}

\section{Anisotropic sampling}
\label{sec:mc-anisotropy}

\TODO{How $\xi$ enters the staple (per-plane coupling ratio $\xi^{\pm 1}$
folded into the staple sum, $\beta$ stays the single overall scale);
$\xi = 1$ is bit-exact backward compatible (unit-tested). Report
\texttt{validate\_anisotropy.py}: exact 2D plaquette reproduced at
$\xi = 1$, plaquette split $\langle P_{st}\rangle > \langle P_{ss}\rangle$,
and the measured renormalized anisotropy $\xi_R$ vs.\ bare $\xi$
(figure \texttt{anisotropy\_validation.png}).}

\section{Error analysis}
\label{sec:mc-errors}

\subsection{Autocorrelation}
\TODO{Integrated autocorrelation time $\tau_{\mathrm{int}}$ with
Madras--Sokal automatic windowing \cite{madras1988sokal}; the rule
$n_{\mathrm{skip}} \gtrsim 2\tau_{\mathrm{int}}$ OF THE TARGET OBSERVABLE
(the smeared glueball operator, not the plaquette). Report the pre-flight
measurement that fixed the production $n_{\mathrm{skip}}$
(figure \texttt{glueball\_autocorrelation.png}).}

\subsection{Jackknife}
\TODO{Leave-one-out and delete-block jackknife; why BLOCKED jackknife for
correlator data; the correlated-difference jackknife (same configs, take
the difference inside each sample) that powers every
GELT-vs-classical significance statement in \cref{ch:glueball}.}
