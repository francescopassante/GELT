% ==============================================================================
%  Chapter 9 -- Conclusions and outlook
%  Purpose: ~5-8 pages. Summary of what was shown, honest limitations, and
%  the roadmap. The future-work list below is the CURRENT plan (2026-07);
%  prune items that get done into their result chapters.
% ==============================================================================

\section{Summary of results}
\label{sec:concl-summary}

\TODO{One paragraph per delivered claim, mirroring
\cref{sec:intro-contributions}: (i) an exactly gauge-equivariant
attention architecture with configuration-dependent receptive fields;
(ii) validated samplers and a classical spectroscopy pipeline reproducing
exact results and a clean GEVP anchor $m\,\at \approx 0.33$;
(iii) the learned variational operator result -- same mass as the
classical GEVP ($m\,\at = 0.332 \pm 0.027$), better ground-state overlap
($\Delta A_0 = +0.078 \pm 0.022$, $3.6\sigma$, two independent
ensembles), with the enlarged GEVP collapsing onto the learned operator;
(iv) \TODO{interpretability findings once they exist}.}

\section{Limitations}
\label{sec:concl-limitations}

\TODO{Say them before the committee does: bare (untuned) anisotropy, so
$m\,\as \approx 1.0$ at $\beta_s = 0.8$ is not continuum physics;
$\SU{2}$ only, single $(\beta, \xi, L)$ point; the L-CNN column of the
variational shootout is missing; the trained (RoPE) variant has weaker
test coverage than the bias variant; no worst-case-$\Omega$ stress test;
regression shootout (Ch.~6) not yet systematic.}

\section{Future work}
\label{sec:concl-future}

\TODO{Ordered roadmap (tracks notes/fable\_audit.md; update as items
land):}
\begin{enumerate}
  \item \textbf{Architecture hygiene:} merge \texttt{blocks\_bias} /
        \texttt{blocks\_rope} into one module with a
        \texttt{pos\_encoding} switch; parametrize equivariance tests
        over all variants; enforce RoPE coverage $d_{qkv} \ge 2D$;
        worst-case-$\Omega$ stress test.
  \item \textbf{Scaling engineering:} offset-chunked attention +
        on-the-fly transport -- the memory gate on the whole
        interpretability program at physical $R$.
  \item \textbf{Complete the shootouts:} matched-parameter L-CNN on the
        per-timeslice variational glueball task; systematic regression
        comparison (Ch.~6).
  \item \textbf{Interpretability program} (Ch.~8): $\beta$ in the
        datasets, cooling/smearing of $q(x)$, then the three studies.
  \item \textbf{Toward QCD:} $\U{1}$ and $\SU{3}$ samplers
        (Cabibbo--Marinari heat bath), 3+1D production, anisotropy
        tuning (renormalized $\xi$), continuum extrapolation of the
        $0^{++}$ mass; further channels ($2^{++}$, $0^{-+}$) via
        irrep-projected learned operators.
  \item \TODO{Longer horizon, if space permits: learned operators for
        multi-level algorithms, transfer across $\beta$/volumes,
        generative directions.}
\end{enumerate}

\section{Closing perspective}
\label{sec:concl-closing}

\TODO{One page: the broader claim -- exact symmetry + adaptive attention
turns a neural network from a black-box regressor into a variational
instrument whose internal state (the attention map) is itself a physical
measurement. Where this sits in the ML-for-LGT landscape.}
