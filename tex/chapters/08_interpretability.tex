% ==============================================================================
%  Chapter 8 -- Interpreting attention as a physical measurement
%  Purpose: the thesis NOVELTY chapter (per notes/explainability.md), ~15-20
%  pages. Almost entirely FUTURE WORK as of 2026-07: the hooks exist
%  (_last_alpha / _last_score are stashed per layer), the studies do not.
%  This skeleton fixes the three studies and their validation targets so the
%  experiments can be run against a fixed plan.
% ==============================================================================

\section{Why equivariant attention is interpretable}
\label{sec:interp-why}

\TODO{The chapter thesis: because the attention score is gauge INVARIANT
by construction, the attention map $\alpha(x, \Delta x)$ is a legitimate
physical observable -- a measurement the network performs on the
configuration, not an artifact of a gauge choice. Contrast with
interpreting raw weights of a non-equivariant network (gauge-variant,
meaningless). Define the extraction protocol:
\texttt{\_last\_alpha} per layer/head under \texttt{no\_grad}.}

\section{Study 1: emergent correlation length}
\label{sec:interp-xi}

\TODO{PLANNED. Define an attention range $\ell_{\mathrm{att}}$ (e.g.\ the
decay length or first moment of $\alpha$ vs.\ $|\Delta x|$), measure it
across $\beta$, and compare with the physical correlation length
$\xi(\beta)$ from lattice ground truth. Strong version needs
$\beta$-conditioned training $\Rightarrow$ $\beta$ must enter the dataset
(blocker recorded in \cref{sec:concl-future}). Deliverable figure:
$\ell_{\mathrm{att}}(\beta)$ vs.\ $\xi(\beta)$.}

\section{Study 2: localization on topological structure}
\label{sec:interp-topology}

\TODO{PLANNED. For a network trained on $q(x)$: overlay attention mass on
the cooled/smeared topological charge density; quantify with a
correlation coefficient between per-site attention received and
$|q(x)|$ after cooling. Blocker: cooling/smearing of $q(x)$ is not
implemented yet. Include an intervention test (ablate heads / zero
attention on lump sites and measure the regression degradation) so the
claim is causal, not correlational.}

\section{Study 3: head and layer specialization}
\label{sec:interp-heads}

\TODO{PLANNED. Per-head statistics of $\alpha$ (range, anisotropy,
entropy) across layers; head-ablation matrix; do RoPE and bias variants
specialise differently? Deliverable: a specialization taxonomy figure.}

\section{Validation methodology}
\label{sec:interp-validation}

\TODO{The discipline that separates this from ML-paper attention-gazing:
every claim validated against a lattice ground truth ($\xi(\beta)$ from
correlators, cooled $q(x)$) and backed by an intervention. State the
null results policy: negative findings get reported.}

\section{Compute constraints}
\label{sec:interp-compute}

\TODO{The memory gate: physical receptive fields need larger $R$, which
the current materialised $L^1$-ball transport cannot afford --
offset-chunked attention + on-the-fly transport is the enabling
engineering item (\cref{sec:concl-future}).}
